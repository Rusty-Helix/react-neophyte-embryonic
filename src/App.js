import Header from './components/Header'


// App is also a component
function App() { // same as the file name


  return (
    // this can only return 1 element
    <div className="Container">
      <Header />
      
    </div>
    // jsx expression must have one element
  );
}

export default App;
